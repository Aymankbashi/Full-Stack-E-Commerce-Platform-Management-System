# شرح تفصيلي لمشروع المتجر الإلكتروني

## 1. مقدمة

هذا المشروع هو متجر إلكتروني متكامل تم تطويره باستخدام إطار عمل Laravel. المشروع يوفر نظامًا متكاملًا لإدارة المتجر الإلكتروني مع واجهة مستخدم متعددة اللغات ودعم كامل للغة العربية.

## 2. الهيكل العام للمشروع

### 2.1. المجلدات الرئيسية

- **app**: يحتوي على الكود الأساسي للتطبيق
  - **Http**: يحتوي على المتحكمات (Controllers) والوسائط (Middleware)
  - **Models**: يحتوي على نماذج قاعدة البيانات
  - **Providers**: يحتوي على مزودي الخدمات (Service Providers)

- **config**: يحتوي على ملفات الإعدادات
- **public**: يحتوي على الملفات العامة مثل الصور والملفات الثابتة
- **resources**: يحتوي على المشاهدات (Views) والموارد الأخرى
- **routes**: يحتوي على ملفات تعريف المسارات (Routes)
- **storage**: يحتوي على الملفات المؤقتة
- **tests**: يحتوي على ملفات الاختبارات

### 2.2. ملفات المشروع الرئيسية

- **composer.json**: يحتوي على تعريفات الحزم والمتطلبات
- **artisan**: أداة سطر أوامر Laravel
- **bootstrap/app.php**: نقطة الدخول للتطبيق
- **public/index.php**: نقطة الدخول للطلب HTTP

## 3. نظام المستخدمين والأدوار

### 3.1. نماذج المستخدمين والأدوار

- **User Model**: يمثل نموذج المستخدم في قاعدة البيانات
  - يحتوي على سمات مثل name, email, password, role
  - يحتوي على علاقات مع الأدوار والطلبات

- **Role Model**: يمثل نموذج الدور في قاعدة البيانات
  - يحتوي على سمات مثل name, description
  - يحتوي على علاقات مع المستخدمين والأذونات

- **Permission Model**: يمثل نموذج الإذن في قاعدة البيانات
  - يحتوي على سمات مثل name, description
  - يحتوي على علاقات مع الأدوار

### 3.2. نظام المصادقة

- **AuthController**: متحكم إدارة المصادقة
  - showLoginForm: عرض نموذج تسجيل الدخول
  - login: معالجة طلب تسجيل الدخول
  - showRegistrationForm: عرض نموذج التسجيل
  - register: معالجة طلب التسجيل
  - logout: معالجة طلب تسجيل الخروج

- **Middleware**: الوسائط المستخدمة في نظام المصادقة
  - auth: التحقق من تسجيل المستخدم الدخول
  - admin: التحقق من أن المستخدم هو مدير

### 3.3. نظام التسجيل

- **RegisterController**: متحكم إدارة التسجيل
  - register: إنشاء مستخدم جديد
  - create: إنشاء مستخدم جديد في قاعدة البيانات

## 4. نظام المنتجات

### 4.1. نموذج المنتج

- **Product Model**: يمثل نموذج المنتج في قاعدة البيانات
  - يحتوي على سمات مثل name, price, description, category
  - يحتوي على علاقات مع الطلبات والمراجعات
  - يحتوي على دوال مساعدة مثل getPriceWithDiscount

### 4.2. متحكم المنتجات

- **ProductController**: متحكم إدارة المنتجات
  - index: عرض قائمة المنتجات
  - show: عرض تفاصيل منتج معين
  - create: إنشاء منتج جديد
  - store: حفظ منتج جديد في قاعدة البيانات
  - edit: تعديل منتج معين
  - update: تحديث منتج في قاعدة البيانات
  - delete: حذف منتج من قاعدة البيانات

### 4.3. مشاهدات المنتجات

- **index.blade.php**: عرض قائمة المنتجات مع إمكانية البحث والتصفية
- **show.blade.php**: عرض تفاصيل منتج معين مع المراجعات
- **create.blade.php**: عرض نموذج إنشاء من جديد
- **edit.blade.php**: عرض نموذج تعديل منتج

## 5. نظام السلة

### 5.1. متحكم السلة

- **CartController**: متحكم إدارة السلة
  - index: عرض محتويات السلة
  - add: إمنتج إلى السلة
  - update: تحديث كمية منتج في السلة
  - remove: إزالة منتج من السلة

### 5.2. إدارة السلة

- تستخدم الجلسات (Sessions) لتخزين محتويات السلة
- يتم تخزين معرف المنتج والكمية والسعر
- يتم حساب إجمالي السعر والخصومات تلقائيًا

## 6. نظام الطلبات

### 6.1. نموذج الطلب

- **Order Model**: يمثل نموذج الطلب في قاعدة البيانات
  - يحتوي على سمات مثل user_id, total, status
  - يحتوي على علاقات مع المستخدمين وعناصر الطلبات

- **OrderItem Model**: يمثل نموذج عنصر الطلب في قاعدة البيانات
  - يحتوي على سمات مثل order_id, product_id, quantity, price
  - يحتوي على علاقات مع الطلبات والمنتجات

### 6.2. متحكم الطلبات

- **OrderController**: متحكم إدارة الطلبات
  - index: عرض قائمة الطلبات للمستخدم
  - show: عرض تفاصيل طلب معين
  - store: إنشاء طلب جديد
  - update: تحديث حالة الطلب

## 7. نظام الدعم الفني

### 7.1. نماذج الدعم الفني

- **SupportSession Model**: يمثل نموذج جلسة الدعم في قاعدة البيانات
  - يحتوي على سمات مثل user_id, agent_id, status
  - يحتوي على علاقات بالمستخدمين وموظفي الدعم

- **SupportMessage Model**: يمثل نموذج رسالة الدعم في قاعدة البيانات
  - يحتوي على سمات مثل session_id, user_id, message
  - يحتوي على علاقات بجلسات الدعم

### 7.2. متحكمات الدعم الفني

- **SupportController**: متحكم نظام الدعم الفني للمستخدمين
  - landing: عرض صفحة الدعم الفني
  - index: عرض قائمة جلسات الدعم
  - createSession: إنشاء جلسة دعم جديدة
  - chat: عرض محادثة الدعم
  - sendMessage: إرسال رسالة في الدردشة
  - endSession: إنهاء جلسة الدعم

- **SupportAdminController**: متحكم نظام الدعم الفني لموظفي الدعم
  - dashboard: لوحة تحكم موظفي الدعم
  - showSession: عرض جلسة دعم معينة
  - professionalChat: عرض محادثة احترافية
  - assignSession: تخصيص جلسة لموظف
  - sendMessage: إرسال رسالة في الدردشة
  - addReply: إضافة رد على رسالة
  - closeSession: إغلاق جلسة الدعم

### 7.3. نظام الردود الجاهزة

- **ReplyTemplate Model**: يمثل نموذج الردود الجاهزة
- **ReplyTemplateController**: متحكم إدارة الردود الجاهزة
- يسمح لموظفي الدعم بإنشاء وتعديل وحذف الردود الجاهزة

## 8. لوحة تحكم الإدارة

### 8.1. متحكم الإدارة

- **AdminController**: متحكم لوحة تحكم الإدارة
  - dashboard: عرض لوحة تحكم الإدارة
  - users: عرض قائمة المستخدمين
  - showUser: عرض تفاصيل مستخدم معين
  - updateUserRole: تحديث دور المستخدم
  - updateUserStatus: تحديث حالة المستخدم
  - products: إدارة المنتجات
  - orders: إدارة الطلبات

### 8.2. مشاهدات الإدارة

- **dashboard.blade.php**: لوحة تحكم الإدارة الرئيسية
- **users.blade.php**: إدارة المستخدمين
- **users/show.blade.php**: عرض تفاصيل مستخدم
- **products.blade.php**: إدارة المنتجات
- **orders.blade.php**: إدارة الطلبات

## 9. نظام المراجعات

### 9.1. نموذج المراجعة

- **Review Model**: يمثل نموذج المراجعة في قاعدة البيانات
  - يحتوي على سمات مثل user_id, product_id, rating, comment
  - يحتوي على علاقات بالمستخدمين والمنتجات

### 9.2. متحكم المراجعات

- **ProductReviewController**: متحكم إدارة المراجعات
  - store: إنشاء مراجعة جديدة
  - update: تحديث مراجعة موجودة
  - delete: حذف مراجعة

## 10. نظام اللغات

### 10.1. دعم اللغات

- يد المشروع اللغتين العربية والإنجليزية
- يتم تحديد اللغة من خلال عنوان الطلب (Accept-Language)
- يتم تخزين النصوص في ملفات لغة في مجلد lang

### 10.2. نظام الترجمة

- يتم تخزين النصوص المترجمة في ملفات PHP في مجلد lang
- يتم استخدام دالة trans() لعرض النصوص المترجمة
- يتم دعم اتجاه النص (RTL/LTB) حسب اللغة

## 11. نظام البحث

### 11.1. البحث عن المنتجات

- يتم البحث عن المنتجات بناءً على الاسم أو التصنيف أو الوصف
- يتم استخدام استعلامات Eloquent للبحث
- يتم عرض نتائج البحث في صفحة منفصلة

### 11.2. البحث المتقدم

- يدعم البحث حسب الفئات
- يدعم البحث حسب النطاق السعري
- يدعم البحث حسب التقييم

## 12. نظام العروض

### 12.1. عرض العروض

- يتم عرض المنتجات المخفضة في صفحة منفصلة
- يتم عرض نسبة الخصم بوضوح
- يتم عرض تاريخ انتهاء العرض

### 12.2. إدارة العروض

- يمكن للمديرين إضافة عروض جديدة
- يمكن تحديث العروض الحالية
- يمكن حذف العروض المنتهية

## 13. نظام الإشعارات

### 13.1. أنواع الإشعارات

- إشعارات تسجيل الدخول
- إشعارات إنشاء الطلبات
- إشعارات تحديث حالة الطلب
- إشعارات الردود على الدعم الفني

### 13.2. نظام الإشعارات

- يتم استخدام نظام الإشعارات في Laravel
- يتم إرسال الإشعارات عبر البريد الإلكتروني
- يمكن إضافة قنوات إشعارات أخرى مثل SMS

## 14. الأمان

### 14.1. حماية المسارات

- يتم استخدام الوسائط (Middleware) لحماية المسارات
- يتم التحقق من صحة المستخدم قبل السماح بالوصول
- يتم التحقق من صلاحيات المستخدم

### 14.2. حماية البيانات

- يتم استخدام الـ CSRF لحماية النماذج
- يتم تهيئة كلمات المرور بشكل آمن
- يتم التحقق من صحة البيانات المدخلة

## 15. الأداء

### 15.1. تحسين الأداء

- استخدام ذاكرة التخزين المؤقت (Cache) لتحسين الأداء
- استخدام التخزين المؤقت للصفحات الثابتة
- استخدام التخزين المؤقت للاستعلامات المتكررة

### 15.2. تحسين قاعدة البيانات

- استخدام الفهارس لتحسين سرعة الاستعلامات
- تحسين استعلامات Eloquent
- استخدام الترحيل (Pagination) للبيانات الكبيرة

## 16. الاختبارات

### 16.1. أنواع الاختبارات

- اختبارات الوحدة (Unit Tests)
- اختبارات التكامل (Integration Tests)
- اختبارات المتصفح (Browser Tests)

### 16.2. إجراء الاختبارات

- استخدام PHPUnit لإجراء الاختبارات
- استخدام Laravel Dusk لاختبارات المتصفح
- استخدام Mockery لاختبارات الوحدة

## 17. النشر

### 17.1. إعداد البيئة

- إعداد خادم الويب (Apache/Nginx)
- إعداد قاعدة البيانات
- إعداد الإعدادات البيئية

### 17.2. خطوات النشر

- استنساخ المستودع
- تثبيه التبعيات
- إعداد الملفات القابلة للكتابة
- تشغيل المigrate
- تشغيل الـ Seeders

## 18. الصيانة

### 18.1. تحديثات النظام

- تحديث إطار العمل Laravel
- تحديث الحزم المطلوبة
- تحديث قاعدة البيانات

### 18.2. المراقبة والصيانة

- مراقبة أداء النظام
- مراقبة الأخطاء
- النسخ الاحتياطي للبيانات

## 19. المستقبل

### 19.1. الميزات المستقبلية

- تطبيق جوال
- نظام الدفع المتقدم
- نظام التوصيات
- نظام الإحالات

### 19.2. التحسينات المستقبلية

- تحسين الأداء
- تحسين تجربة المستخدم
- تحسين الأمان
- تحسين التصميم

## 20. الخاتمة

هذا المشروع يمثل متجر إلكتروني متكامل مع جميع الميزات الأساسية اللازمة. تم تصميمه ليكون مرنًا وقابلًا للتوسع، مع مراعاة أفضل الممارسات في تطوير الويب باستخدام Laravel. يمكن استخدام هذا المشروع كقاعدة أساسية لتطوير متاجر إلكترونية متقدمة ومخصصة حسب الاحتياجات.
