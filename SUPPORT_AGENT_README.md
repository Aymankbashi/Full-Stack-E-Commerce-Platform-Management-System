# نظام صلاحيات موظفي الدعم الفني

## نظرة عامة
تم إضافة صلاحية منفصلة لموظفي الدعم الفني في النظام، مما يسمح بتمييزهم عن المديرين والمستخدمين العاديين.

## الأدوار المتاحة
1. **user**: مستخدم عادي - يمكنه إنشاء جلسات دعم والدردشة مع موظفي الدعم
2. **support_agent**: موظف دعم فني - يمكنه الوصول إلى لوحة تحكم الدعم الفني وإدارة الجلسات
3. **admin**: مدير - لديه كافة الصلاحيات بما في ذلك إدارة المستخدمين والمنتجات

## كيفية إنشاء حساب موظف دعم فني

### الطريقة الأولى: عبر لوحة تحكم الإدارة
1. سجل الدخول كمسؤول
2. انتقل إلى صفحة `/admin/users`
3. ابحث عن المستخدم المراد تعيينه كموظف دعم
4. قم بتغيير دوره من "مستخدم" إلى "موظف دعم"

### الطريقة الثانية: عبر التسجيل مع معلمة خاصة
عند التسجيل، أضف المعلمة `support_agent=1` إلى نموذج التسجيل:
```
POST /register
{
    "name": "اسم الموظف",
    "email": "email@example.com",
    "password": "password",
    "password_confirmation": "password",
    "support_agent": "1"
}
```

### الطريقة الثالثة: باستخدام Seeder
قم بتشغيل الأمر التالي لإنشاء حساب موظف دعم تجريبي:
```bash
php artisan db:seed --class=SupportAgentSeeder
```

بيانات الدخول الافتراضية:
- البريد الإلكتروني: support@example.com
- كلمة المرور: password123

## الصلاحيات

### صلاحيات موظف الدعم الفني
- الوصول إلى لوحة تحكم الدعم الفني (`/support/agent/dashboard`)
- عرض جميع الجلسات النشطة
- تعيين الجلسات لنفسه
- إرسال رسائل في الجلسات
- إغلاق الجلسات
- عرض التقارير والإحصائيات

### صلاحيات المدير
- جميع صلاحيات موظف الدعم الفني
- إدارة المستخدمين
- إدارة المنتجات
- تغيير صلاحيات المستخدمين

## المسارات المحمية

### مسارات موظفي الدعم الفني
جميع المسارات التالية تتطلب صلاحية `support_agent` أو `admin`:
- `/support/agent/dashboard` - لوحة تحكم الدعم الفني
- `/support/agent/session/{id}` - عرض تفاصيل جلسة
- `/support/agent/assign/{id}` - تعيين جلسة لموظف
- `/support/agent/send/{id}` - إرسال رسالة
- `/support/agent/close/{id}` - إغلاق جلسة
- `/support/agent/messages/{id}` - الحصول على رسائل جديدة

## التحقق من الصلاحيات

### في الكود
```php
if (Auth::check() && Auth::user()->role === 'support_agent') {
    // الكود الخاص بموظفي الدعم
}
```

### في العرض (Blade)
```blade
@if(auth()->check() && auth()->user()->role === 'support_agent')
    <!-- المحتوى الخاص بموظفي الدعم -->
@endif
```

## ملاحظات مهمة
- يمكن للمديرين الوصول إلى جميع مسارات موظفي الدعم الفني
- يتم توجيه موظفي الدعم الفني تلقائياً إلى لوحة تحكم الدعم الفني بعد تسجيل الدخول
- يمكن تحويل أي مستخدم عادي إلى موظف دعم فني من خلال لوحة تحكم الإدارة
