# نظام الدعم الفني

## نظرة عامة
هذا النظام يتيح للمستخدمين التواصل مع فريق الدعم الفني لحل مشاكلهم. يتم إنشاء جلسة دردشة جديدة لكل مشكلة، ويتم حذف الدردشة تلقائياً بمجرد انتهاء الجلسة.

## المميزات
- إنشاء جلسات دعم جديدة مع تحديد الأولوية والفئة
- دردشة فورية مع موظفي الدعم
- حذف تلقائي للدردشة عند انتهاء الجلسة
- تحديث تلقائي للرسائل الجديدة
- واجهة عربية بالكامل

## التثبيت

### 1. تشغيل الهجرات
قم بتشغيل الأوامر التالية لإنشاء الجداول في قاعدة البيانات:

```bash
php artisan migrate
```

### 2. المسارات المتاحة
- `/support` - صفحة الدعم الفني الرئيسية
- `/support/create` - إنشاء جلسة دعم جديدة
- `/support/chat/{id}` - صفحة الدردشة
- `/support/send/{id}` - إرسال رسالة جديدة
- `/support/end/{id}` - إنهاء الجلسة
- `/support/messages/{id}` - الحصول على الرسائل الجديدة (API)

## الاستخدام

### إنشاء جلسة دعم جديدة
1. انتقل إلى صفحة `/support`
2. املأ النموذج ببيانات المشكلة
3. اضغط على زر "بدء المحادثة"

### الدردشة مع موظف الدعم
1. بعد إنشاء الجلسة، ستنتقل تلقائياً إلى صفحة الدردشة
2. اكتب رسالتك واضغط على زر "إرسال"
3. سيتم تحديث المحادثة تلقائياً كل 3 ثواني

### إنهاء الجلسة
1. اضغط على زر "إنهاء الجلسة" في صفحة الدردشة
2. سيتم حذف جميع الرسائل تلقائياً
3. ستنتقل إلى صفحة الدعم الرئيسية

## النماذج (Models)

### SupportSession
يمثل جلسة الدعم ويتضمن:
- معرف المستخدم
- معرف موظف الدعم
- موضوع المشكلة
- حالة الجلسة (نشطة، مغلقة، ملغاة)
- أولوية المشكلة
- فئة المشكلة
- تاريخ البداية والنهاية

### ChatMessage
يمثل رسالة في الدردشة ويتضمن:
- معرف الجلسة
- معرف المرسل
- نص الرسالة
- حالة القراءة

## التحكم في الجلسات

### إنهاء الجلسة وحذف الرسائل
```php
$session = SupportSession::find($sessionId);
$session->closeSession();
```

### التحقق من حالة الجلسة
```php
$session = SupportSession::find($sessionId);
if ($session->isActive()) {
    // الجلسة نشطة
}
```

## ملاحظات
- يجب أن يكون المستخدم مسجل الدخول للوصول إلى نظام الدعم
- يتم حذف الرسائل تلقائياً عند إنهاء الجلسة
- يمكن للمستخدم إنهاء الجلسة في أي وقت
