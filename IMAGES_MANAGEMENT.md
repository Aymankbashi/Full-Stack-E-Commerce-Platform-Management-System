# إدارة صور المنتجات في المتجر

## نظرة عامة

تمت إضافة نظام متكامل لإدارة صور المنتجات في متجرك، حيث يمكن ربط الصور الموجودة في مجلد التخزين مع المنتجات بناءً على أسمائها، أو تعيين صور عشوائية للمنتجات التي لا تحتوي على صور.

## كيفية العمل

### 1. ربط الصور حسب الاسم

النظام يقوم تلقائياً بربط الصور بالمنتجات بناءً على تطابق أسمائها:
- يقارن اسم كل منتج مع أسماء ملفات الصور
- إذا وجدت صورة تحتوي على جزء من اسم المنتج، يتم ربطها به
- على سبيل المثال، المنتج "لابتوب" سيتم ربطه بالصورة "laptop.jpg"

### 2. الصور العشوائية

المنتجات التي لا تملك صورة مطابقة ستتمكن من الحصول على صور عشوائية من مجموعة الصور المتاحة.

## كيفية الاستخدام

### 1. من خلال واجهة الويب

1. سجل الدخول إلى لوحة تحكم المدير
2. انتقل إلى قسم "صور المنتجات" عبر الرابط: `/admin/product-images`
3. ستظهر لك جميع المنتجات مع صورها الحالية
4. لتحديث الصور حسب الأسماء، انقر على زر "تحديث الصور حسب الاسم"
5. لتعيين صور عشوائية للمنتجات التي لا تملك صور، انقر على زر "تعيين صور عشوائية"

### 2. من خلال سطر الأوامر

يمكنك استخدام الأمر التالي لتشغيل عملية تحديث الصور:

```bash
php artisan products:update-images
```

### 3. من خلال قاعدة البيانات

يمكنك تشغيل الـ seeder لتحديث صور المنتجات:

```bash
php artisan db:seed --class=ProductImageSeeder
```

## مكان تخزين الصور

الصور يتم تخزينها في المسار التالي:

```
storage/app/public/products/
```

يجب أن تكون هذه الصورة متاحة للجمهور عبر الرابط:

```
/storage/products/{اسم_الملف}
```

## ملاحظات هامة

1. يجب أن تكون أسماء الصور تحتوي على جزء من اسم المنتج لكي يتم ربطها تلقائياً
2. إذا لم يتم العثور على صورة مطابقة، سيتم تعيين صورة عشوائية (إن وجدت)
3. يمكن تعديل منطق البحث عن الصور في الملفات التالية:
   - `app/Console/Commands/UpdateProductImages.php`
   - `app/Http/Controllers/ProductImageController.php`
   - `database/seeders/ProductImageSeeder.php`

## تحسينات مستقبلية

يمكن تطوير هذه الميزة ليشمل:
1. دعم تحميل الصور مباشرة من واجهة الويب
2. إضافة معاينة للصور قبل التعيين
3. إضافة ميزة السحب والإفلات لتحميل الصور
4. تحسين حجم الصور لتحميل أسرع
